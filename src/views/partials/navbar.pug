nav(class='navbar navbar-dark sticky-top bg-dark flex-md-nowrap p-0')
  a(class='navbar-brand col-sm-3 col-md-2 mr-0' href='/') Electra Bay
  input(
    aria-label='Search'
    autocomplete='off'
    class='form-control form-control-dark w-100'
    placeholder='Search'
    type='text'
  )
  if me
    ul(class='navbar-nav px-3')
      li(class='nav-item text-nowrap')
        a(class='nav-link' href='/logout' aria-label='Log out')
          svg(class='icon')
            use(xlink:href='/sprites/open-iconic.svg#account-logout')
  else
    ul(class='navbar-nav px-3')
      li(class='nav-item text-nowrap')
        a(class='nav-link' href='/login' aria-label='Open login page')
          svg(class='icon')
            use(xlink:href='/sprites/open-iconic.svg#account-login')
